{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the EITEL launcher window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
